global:
  resolve_timeout: 5m

route:
  receiver: null-receiver
  group_by:
    - alertname
    - instance
    - service
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: null-receiver